"""Per-task theory snippets shown on the playground and in the level theory TOC."""

from __future__ import annotations

TASK_THEORY: dict[str, str] = {
    "init_repo": """\
### Зачем `git init`

В реальном проекте первый шаг — превратить папку с кодом в **репозиторий Git**.
Появляется скрытый каталог `.git` с историей. Без него Git не знает, какие файлы
отслеживать и какие коммиты уже были.

**Бизнес-кейс:** вы клонируете шаблон или создаёте пустую папку для нового сервиса —
перед первым коммитом нужен `git init` (или `git clone`, который делает это за вас).
""",
    "first_commit": """\
### Первый коммит

Коммит — это **снимок** проекта с сообщением. Сначала файл попадает в рабочую папку,
затем в индекс (staging), затем в историю. Сообщение коммита читают коллеги в code review
и в `git log`.

**Бизнес-кейс:** вы добавили README и конфиг — фиксируете «начальная структура проекта»,
чтобы команда могла откатиться к этой точке.
""",
    "check_status": """\
### `git status` — карта репозитория

Команда показывает, что изменилось, что уже в индексе и что Git ещё не отслеживает.
Это первая диагностика перед коммитом или перед сдачей задачи в CI.

**Бизнес-кейс:** перед пушем вы проверяете, не осталось ли случайных правок
в `hello.txt`, которые не должны попасть в релиз.
""",
    "stage_unstage": """\
### Индекс: подготовить и отменить подготовку

Staging — это «корзина» перед коммитом. Можно подготовить файл и передумать,
**сняв его с индекса**, не трогая содержимое на диске.

**Бизнес-кейс:** вы случайно сделали `add` на весь каталог, но в коммит нужен
только один файл — снимаете лишнее с индекса.
""",
    "view_diff": """\
### Diff — что именно изменилось

`git diff` показывает построчные отличия между рабочей папкой и последним коммитом
(или между индексом и коммитом). Без diff легко закоммитить не то.

**Бизнес-кейс:** ревьюер просит «покажи, что поменялось в конфиге» — diff даёт
точный ответ до merge request.
""",
    "commit_second": """\
### Вторая ревизия

История Git — цепочка коммитов. Каждый новый коммит ссылается на предыдущий.
Осмысленные сообщения помогают искать, когда появилась регрессия.

**Бизнес-кейс:** после первого коммита вы дополняете `hello.txt` — фиксируете
отдельным коммитом с понятным сообщением.
""",
    "amend_commit": """\
### Amend — поправить последний коммит

Если забыли файл или опечатались в сообщении **последнего** коммита (и он ещё не
ушёл на общий remote), можно изменить его состав через amend.

**Бизнес-кейс:** в коммит «добавить логирование» забыли `config.txt` — дополняете
индекс и amend вместо лишнего коммита «fix oops».
""",
    "view_history": """\
### Компактная история

`git log --oneline` сжимает историю до одной строки на коммит: короткий хеш и сообщение.
Удобно для быстрой ориентации в ветке.

**Бизнес-кейс:** перед cherry-pick вы смотрите последние 10 коммитов и выбираете нужный SHA.
""",
    "grep_in_repo": """\
### Поиск по отслеживаемым файлам

`git grep` ищет текст **только в файлах, которые уже в Git** (не в untracked).
Результат можно сохранить в файл через перенаправление `>`, как в обычном терминале.

**Бизнес-кейс:** нужно найти, в каком модуле встречается строка `Git`, и сохранить
одну строку отчёта для тикета — `git grep` + запись в `grep-hit.txt`.
""",
    "stage_tracked_only": """\
### Коммит только отслеживаемых файлов

Иногда в папке лежат черновики и временные файлы. Флаг «только уже отслеживаемые»
позволяет закоммитить правки в известных файлах, не подхватывая мусор.

**Бизнес-кейс:** рядом с `hello.txt` лежит локальный `scratch.txt` — в коммит уходит
только правка в отслеживаемом файле.
""",
    "reset_head_unstage": """\
### Снять с индекса через reset

`git reset` по имени файла убирает его из staging, оставляя правки в рабочей папке.
Это альтернатива `restore --staged` в старых привычках команд.

**Бизнес-кейс:** вы подготовили файл к коммиту, но решили включить его в следующий
коммит вместе с другими правками.
""",
    "diff_cached_staged": """\
### Staged diff

`git diff --cached` показывает, что **уже в индексе** и попадёт в следующий коммит.
Полезно перед финальным `commit`, чтобы не удивиться содержимому снимка.

**Бизнес-кейс:** перед релизным коммитом проверяете staged-изменения и оставляете
маркер `staged-ready.txt`, что проверка выполнена.
""",
}


def theory_for_task(slug: str) -> str:
    return TASK_THEORY.get(slug, "")
