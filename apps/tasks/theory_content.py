"""Level learning content: theory markdown, section hints, and mermaid diagrams.

Source of truth for initial TheoryBlock seeding. Imported by seed_initial_data,
sync_theory_content, and the theory view so data is not tied to a management command module.
"""

THEORY_CONTENT = {
    1: """# Основы Git: первые шаги

Git — система контроля версий, которая хранит **историю снимков** вашего проекта.
На этом уровне вы освоите ментальную модель «три зоны», цикл состояний файлов и
первые команды: от `git init` до просмотра истории и справки.

Для новичка типичная путаница — считать, что «сохранил файл» и «сделал коммит» это одно
и то же. На практике между ними всегда есть **индекс** и ваш осознанный выбор, что
включать в снимок. Читайте теорию без спешки и сразу пробуйте команды в песочнице:
так материал запоминается лучше, чем от заучивания флагов.

## Ментальная модель: три зоны

Представьте Git как конвейер подготовки «сохранений». Ниже — три зоны в виде таблицы;
вернитесь к ней, если в `git status` что-то кажется непонятным.

| Зона | Другое имя | Что здесь |
| --- | --- | --- |
| **Рабочий каталог** (working tree) | рабочая копия | Файлы на диске, с которыми вы редактируете код прямо сейчас |
| **Индекс** (staging area) | буферная зона | Список изменений, **готовых** попасть в следующий коммит |
| **Репозиторий** (.git) | история | Неизменяемые **коммиты** — снимки дерева файлов плюс метаданные |

**Коммит** — это не «разница с прошлым», а **полный снимок** дерева на момент
фиксации (плюс ссылки на родителей, автор, сообщение). Git умеет хранить данные
эффективно, но логически вы думаете именно **снимками**, а не «патчами поверх
патчей».

### Модель продвижения изменений

Изменения движутся по цепочке:

```
Рабочий каталог  →  git add  →  Индекс  →  git commit  →  Локальный репозиторий
                                                              ↓
                                                    git push (уровень 5)
                                                              ↓
                                                    Удалённый репозиторий
```

- **`git add`** — «продвинуть» правки из рабочего каталога в индекс.
- **`git commit`** — «продвинуть» содержимое индекса в новый коммит.
- **`git push`** — отправить коммиты на сервер (изучим на уровне 5).

Пока файл не добавлен в индекс, он **не войдёт** в следующий коммит, даже если
вы его отредактировали.

## Цикл состояний файла

Каждый отслеживаемый файл находится в одном из состояний:

| Состояние | Смысл |
| --- | --- |
| **UNTRACKED** | Git ещё не следит за файлом (новый файл, не в индексе) |
| **MODIFIED** | Изменён в рабочем каталоге, но **не** в индексе |
| **STAGED** | Изменение **в индексе**, готово к коммиту |
| **COMMITTED** | Зафиксировано в истории |

Типичный цикл для одного файла:

```
UNTRACKED  ──git add──►  STAGED  ──git commit──►  COMMITTED
     ▲                      │
     │                      │ правка файла
     └──── git add ─────────┘
```

После коммита файл снова «чистый» в рабочем каталоге, пока вы его не измените.
Команда **`git status`** показывает, где сейчас каждый файл — начинайте с неё.

Задача **`check_status`** тренирует чтение вывода `git status`; **`stage_unstage`**
— осознанное добавление и снятие с индекса.

## Базовые команды

### `git init`

**Определение:** создаёт каталог `.git` и пустой репозиторий в текущей папке.
Без этого Git не отслеживает историю.

```bash
git init
```

### `git add`

**Определение:** переносит изменения из рабочего каталога в **индекс**.

| Форма | Назначение |
| --- | --- |
| **`git add файл`** | Добавить конкретный файл |
| **`git add .`** | Все изменения в текущем каталоге и подкаталогах |
| **`git add -u`** | Только **уже отслеживаемые** файлы (обновления и удаления), без новых untracked |
| **`git add -A`** | Все изменения во всём репозитории: новые, изменённые и удалённые |
| **`git add -p`** | **Интерактивно по «кускам»** (patch): выбираете, какие фрагменты diff попадут в индекс |

Флаг **`-u`** (`--update`) удобен, когда нужно закоммитить правки существующих
файлов, не захватывая случайно новые. **`-A`** (`--all`) — «взять всё».
**`-p`** помогает собрать аккуратный коммит из части правок в одном файле.

Задачи **`stage_tracked_only`** и **`stage_unstage`** закрепляют различие между
полным и выборочным добавлением.

```bash
git add .
git add -u
git add -p README.md
```

### `git commit`

**Определение:** фиксирует текущее состояние **индекса** как новый коммит.

- **`-m "текст"`** — сообщение коммита в командной строке.
- **`--amend`** — заменить **последний** коммит (добавить забытые файлы или
  исправить сообщение). Подробнее на уровне 4; на старте достаточно знать, что
  amend переписывает конец ветки локально.

```bash
git commit -m "Первый снимок"
git commit --amend -m "Исправленное сообщение"
```

### `git status`

**Определение:** показывает ветку, состояние индекса и рабочего каталога.

- **`--short`** / **`-s`** — компактный формат: два символа на файл
  (индекс / рабочее дерево). Например: `M ` — staged, ` M` — только в рабочей
  копии, `??` — untracked.

```bash
git status
git status --short
```

### `git diff`

**Определение:** показывает различия.

| Команда | Сравнивает |
| --- | --- |
| **`git diff`** | Рабочий каталог ↔ индекс (непроиндексированные правки) |
| **`git diff --staged`** | Индекс ↔ последний коммит (то, что **попадёт** в commit) |
| **`git diff --cached`** | То же, что `--staged` (синоним) |

Задача **`diff_cached_staged`** тренирует именно **`--cached`**: «что я уже
подготовил к коммиту?».

```bash
git diff
git diff --cached
```

### `git restore` и снятие с индекса

- **`git restore файл`** — отменить непроиндексированные правки в рабочем каталоге.
- **`git restore --staged файл`** — убрать файл из индекса, **сохранив** правки
  в рабочем каталоге (бывший `git reset HEAD файл`).

Задача **`reset_head_unstage`** связана с этим приёмом.

```bash
git restore --staged app.py
```

### `git log`

**Определение:** история коммитов.

- **`--oneline`** — одна строка на коммит (короткий SHA + сообщение).
- **`--graph`** — ASCII-граф ветвлений и слияний.
- **`--all`** — все ветки (на старте достаточно знать, что граф показывает
  «форму» истории).

```bash
git log --oneline
git log --graph --oneline
```

### `git grep`

**Определение:** поиск текста **по версиям в репозитории** (быстрее и точнее,
чем обычный grep по рабочей копии, если нужна история или индекс).

```bash
git grep "TODO"
git grep -n "function main"
```

Задача **`grep_in_repo`** закрепляет поиск по tracked-файлам.

### Справка: `git help`

**Определение:** встроенная документация.

```bash
git help commit
git commit --help
man git-add    # на Unix-системах
```

## Пример цепочки для нового проекта

```bash
git init
echo "# Demo" > README.md
git add README.md
git status --short
git diff --cached
git commit -m "Первый снимок"
git log --oneline
```

## Снимки и эффективность хранения

Логически каждый коммит — **снимок** всего дерева проекта. Физически Git
хранит объекты (blob, tree, commit) и **переиспользует** неизменённые части
между коммитами — поэтому репозиторий не раздувается линейно. Думайте
«снимками» при планировании коммитов; «дельты» — внутренняя оптимизация.

## Важные принципы

- Коммитьте **часто** небольшими логичными порциями; «наводить порядок» можно
  позже (rebase, amend — уровень 4).
- Перед рискованным шагом — коммит или stash.
- Цикл: **`status` → `add` → `diff --cached` → `commit` → `log`**.
- Сообщение коммита должно объяснять **зачем**, а не только **что**.
""",
    2: """# Ветки: параллельные линии работы

Ветка — это **именованная ссылка на коммит**, которая двигается вперёд при
каждом новом коммите. **`HEAD`** — указатель «где вы сейчас»: обычно на
именованной ветке, иногда — на отдельном коммите (detached HEAD).

Ветки позволяют работать параллельно: фича, hotfix и эксперимент не мешают друг
другу, пока вы сами не решите их объединить. Это основа командной разработки —
даже если вы учитесь один, привыкайте думать ветками с самого начала.

## Ветки как указатели (refs)

Внутри `.git/refs/heads/` хранятся имена веток; каждое имя указывает на
**последний коммит** этой линии. Создание ветки **не копирует файлы** — только
добавляет новую метку на текущий (или указанный) коммит.

```
main ──► C1 ──► C2 ──► C3
              └──► feature ──► F1 ──► F2
```

Переключение ветки меняет **содержимое рабочего каталога** под выбранный
коммит — без клонирования всего проекта.

## Команды ветвления

### `git branch`

**Определение:** управление локальными ветками.

| Форма | Действие |
| --- | --- |
| **`git branch`** | Список веток; активная помечена `*` |
| **`git branch имя`** | Создать ветку **на текущем** коммите, **не переключаясь** |
| **`git branch -m старое новое`** | Переименовать |
| **`-d` / `-D`** | Удалить (безопасно / принудительно) |
| **`-vv`** | Подробно: последний коммит и **upstream** (отслеживаемая remote-ветка) |

**Создание ветки без переключения** — важный приём: подготовить линию работы,
пока вы остаётесь на `main`. Задача **`branch_without_checkout`** тренирует
именно это.

```bash
git branch feature-login
git branch -vv
git branch hotfix abc1234
```

### `git switch` (современный способ)

**Определение:** явно переключает ветку или коммит.

- **`git switch feature-x`** — перейти на ветку.
- **`git switch -c feature-x`** — создать и переключиться (аналог `checkout -b`).
- **`git switch --detach коммит`** — detached HEAD для просмотра или эксперимента.

```bash
git switch -c feature-x
git switch main
```

### `git checkout` (классический способ)

**Определение:** переключение ветки/коммита; также используется для восстановления
файлов (см. уровень 4). Для **смены ветки** предпочтительнее `git switch`.

- **`git checkout -b имя`** — создать ветку и переключиться.
- **`git checkout коммит`** — detached HEAD.

```bash
git checkout -b boss
git checkout main
```

Задачи **`create_branch`**, **`switch_branch`**, **`list_branches`** закрепляют
базовый цикл.

## Detached HEAD

Если **`HEAD`** указывает **напрямую на коммит**, а не на ветку — вы в
**detached HEAD**:

- Можно смотреть старый код, собирать эксперимент.
- **Новые коммиты** не привязаны к ветке; при переключении они могут стать
  «потерянными» (спасение — **`git reflog`**, уровень 4).

Чтобы сохранить работу: **`git switch -c rescue-branch`** из detached состояния.
Задача **`rescue_detached_head`** тренирует этот сценарий.

```bash
git switch --detach v1.0
# ... правки ...
git switch -c fix-from-v1
```

## Topic-ветки

**Topic branch** (ветка темы) — короткая ветка **на одну задачу**: `fix-login`,
`add-export`, `docs-readme`. Правила:

- Имя отражает цель, не имя разработчика.
- Живёт дни, не месяцы; после merge — удаляется.
- Ответвляется от актуального `main` (или релизной ветки).

```
main ──► ... ──► M5
                  └──► feature/export ──► E1 ──► E2 ──► (merge) ──► main
```

## Ветка от конкретного коммита

Hotfix часто стартует **не с tip main**, а с тега или старого коммита:

```bash
git branch hotfix v1.2.0
git switch hotfix
```

Задача **`branch_from_commit`** закрепляет ответвление от SHA.

## Связь с удалёнными ветками

Локальная ветка может **отслеживать** remote (`upstream`). Это видно в
**`git branch -vv`**: `[origin/main]` — куда по умолчанию уйдёт `git pull` /
`git push`. Подробнее на уровне 5.

## Практика

- Одна ветка — одна цель (topic branch).
- Перед переключением: **`git status`** чистый или **`git stash`**.
- Не работайте долго в detached HEAD без создания ветки.
- После merge удаляйте слитые ветки: **`git branch -d feature-x`**.
""",
    3: """# Слияния, конфликты и интеграция

Когда несколько веток развиваются параллельно, их изменения нужно **влить**
в общую линию. Этот уровень — merge-base, fast-forward, merge-коммиты,
конфликты, cherry-pick и revert.

Слияние — момент, где Git перестаёт быть «личным блокнотом» и становится
инструментом координации. Конфликты здесь — нормальная ситуация, а не признак
поломки: Git просто просит вас явно выбрать, какую версию строки оставить.

## Общий предок: merge-base

**Merge-base** — последний **общий предок** двух веток. Git сравнивает
изменения **от предка** до каждого tip, чтобы понять, что можно слить
автоматически, а где правили **одни и те же строки**.

```bash
git merge-base main feature-x
```

Задача **`merge_base_ready`** знакомит с этой идеей перед сложными merge.

## Fast-forward и merge-коммит

### Fast-forward (FF)

Если целевая ветка **не ушла вперёд** с момента ответвления, Git просто
**передвигает указатель** ветки — отдельный merge-коммит не нужен:

```
До:  main ──► A ──► B
              └──► feature ──► C ──► D

После FF merge feature в main:
     main ──► A ──► B ──► C ──► D
```

Задача **`fast_forward_merge`** тренирует этот сценарий.

### Merge-коммит (non-FF)

Если обе ветки двигались, Git создаёт **merge-коммит с двумя родителями**:

```bash
git switch main
git merge feature-x
```

### `git merge --no-ff`

**Определение:** **запретить fast-forward** — всегда создавать merge-коммит,
даже когда FF возможен. В истории явно видно событие «ветка X влита в main».

```bash
git merge --no-ff feature-x -m "Merge branch 'feature-x'"
```

Задача **`no_ff_merge`** закрепляет `--no-ff`.

### Squash merge

**Squash** — все коммиты ветки **сжимаются в один** на целевой ветке (история
feature при этом не сохраняется как цепочка). На платформах это опция кнопки
Merge; локально — **`git merge --squash`** + один commit.

```bash
git merge --squash feature-x
git commit -m "Add export feature"
```

Задача **`squash_merge`** тренирует squash.

## Конфликты

Конфликт — Git не может автоматически совместить правки **одного участка**
файла. В файле появляются маркеры:

```
<<<<<<< HEAD
наша версия
=======
их версия
>>>>>>> feature-x
```

**Алгоритм:**

1. Открыть файлы с конфликтом, выбрать итоговый текст.
2. Удалить маркеры `<<<<<<<`, `=======`, `>>>>>>>`.
3. **`git add`** каждый исправленный файл.
4. **`git commit`** (или завершить merge).

**`git merge --abort`** — отменить незавершённое слияние и вернуть состояние
«до merge». Задачи **`resolve_conflict`** и **`abort_merge`**.

## rerere (Reuse Recorded Resolution)

**`rerere`** — если Git включён (`rerere.enabled`), он **запоминает**, как вы
разрешили конфликт, и может **применить то же решение** при повторном merge/rebase
того же участка. Полезно при частых rebase длинных веток.

```bash
git config rerere.enabled true
```

## Cherry-pick

**Определение:** переносит **один или несколько** выбранных коммитов на текущую
ветку, создавая **новые** коммиты с тем же diff.

```bash
git cherry-pick abc1234
git cherry-pick A..B   # диапазон (осторожно с границами)
```

Идеально для hotfix: взять патч из `develop`, не сливая всю ветку.
Задача **`cherry_pick_hotfix`**.

## Revert

**Определение:** создаёт **новый коммит**, отменяющий эффект указанного —
**без переписывания** истории. Безопасно для опубликованных веток.

```bash
git revert abc1234
```

### Revert merge-коммита: `-m`

У merge-коммита **два родителя**. **`git revert -m 1`** говорит: «считать
**первого** родителя основной линией (обычно main)» и откатить изменения,
привнесённые второй веткой.

```bash
git revert -m 1 MERGE_SHA
```

Задача **`revert_merge`**.

## Граф истории

```bash
git log --graph --oneline --all --decorate
```

- **`--graph`** — дерево merge.
- **`--all`** — все ветки и теги.
- **`--decorate`** — имена refs рядом с коммитами.

После интеграции **всегда** смотрите граф: нет ли лишних merge, правильно ли
сошлись ветки.

## Рабочий цикл интеграции

1. Обновить целевую ветку (`git switch main` → `git pull`).
2. Обновить feature (`git switch feature-x` → `git merge main` или rebase).
3. Разрешить конфликты, прогнать тесты.
4. Merge в main (локально или через PR/MR на платформе).
5. **`git log --graph --oneline --all`** — проверка.

## Практические правила

- Мелкие коммиты легче мержить и cherry-pick.
- Конфликт — не ошибка Git, а сигнал согласовать изменения людьми.
- На shared-ветках предпочитайте **revert**, а не переписывание истории.
""",
    4: """# История под контролем

Локальная история — черновик: её можно **упорядочить**, **временно спрятать**
правки или **откатиться**. На опубликованных ветках действуйте осторожнее:
золотое правило rebase — не переписывать то, что уже используют другие.

Представьте, что коммиты — это черновики главы книги. Пока глава только у вас,
можно переставлять абзацы. Как только коллеги начали читать вашу версию,
безопаснее добавлять **новую** правку (revert), чем стирать старые страницы.

## Золотое правило rebase

> **Не переписывайте историю коммитов, которые уже запушены и от которых
> работают другие**, без явного согласования команды.

Локально — rebase, amend, squash свободны. На **`main`** / shared-ветках —
**revert**, а не force-push.

## `git commit --amend`

**Определение:** заменяет **последний** коммит: добавляет файлы из индекса
и/или меняет сообщение. SHA коммита **меняется**.

```bash
git add forgotten.py
git commit --amend -m "Новое сообщение"
```

- **`-C HEAD`** — переиспользовать **то же сообщение** без редактора (удобно
  когда забыли файл, а текст коммита оставляете).
- **`--no-edit`** — amend без смены сообщения.

```bash
git add fix typo
git commit --amend -C HEAD --no-edit
```

Задачи **`amend_commit`**, **`amend_message`**.

## Интерактивный rebase

**`git rebase -i`** — переписать серию коммитов: порядок, squash, reword, edit.

```bash
git rebase -i HEAD~5
```

В редакторе — инструкции для каждого коммита:

| Команда | Действие |
| --- | --- |
| **pick** | Оставить как есть |
| **reword** | Изменить сообщение |
| **squash** / **fixup** | Объединить с предыдущим |
| **edit** | Остановиться для правки |
| **drop** | Удалить коммит |

Задачи **`reorder_commits`**, **`squash_commits`**, **`edit_commit`**.

### Rebase поверх другой ветки

```bash
git switch feature-x
git rebase main
```

Перемещает ваши коммиты **поверх** актуального `main`. Конфликты решаются
пошагово (`git rebase --continue` / `--abort`).

## Stash

**Определение:** временный стек **незакоммиченных** изменений.

```bash
git stash push -m "WIP: login form"
git stash list
git stash apply          # применить, оставить в стеке
git stash pop            # применить и удалить
git stash drop stash@{0}
```

- **`git stash -u`** — включая untracked файлы.
- **`git stash branch new-branch`** — создать ветку и применить stash.

Задача **`stash_workflow`**.

## `git reset`

**Определение:** двигает указатель **текущей ветки** на другой коммит.

| Режим | Ветка | Индекс | Рабочий каталог |
| --- | --- | --- | --- |
| **`--soft`** | ← коммит | без изменений | без изменений |
| **`--mixed`** (default) | ← коммит | сброшен | правки остаются unstaged |
| **`--hard`** | ← коммит | сброшен | **как в коммите, правки теряются** |

```bash
git reset --soft HEAD~1    # «распаковать» последний коммит в индекс
git reset --mixed HEAD~1   # распаковать в рабочий каталог
git reset --hard HEAD~1    # опасно: откат файлов
```

Задача **`reset_modes`**.

## `git restore`

**Определение:** восстановление файлов (Git 2.23+).

```bash
git restore --staged file.py   # unstage
git restore file.py            # отменить правки в рабочем каталоге
git restore --source=HEAD~1 file.py
```

## Reflog

**`git reflog`** — журнал перемещений **`HEAD`** и веток: «куда указывал
указатель». Спасает «потерянные» коммиты после reset или detached HEAD.

```bash
git reflog
git switch -c recovered HEAD@{2}
```

Задача **`reflog_recovery`** (уровень 6 пересекается по теме).

## Revert на публичных ветках

Если коммит уже на **`origin/main`**, **`git revert`** безопаснее **`reset`**:

```bash
git revert abc1234
git push
```

История сохраняет и ошибку, и её отмену — важно для аудита.

## Сравнение приёмов

| Задача | Инструмент |
| --- | --- |
| Забыли файл в последний коммит | **`commit --amend`** |
| Упорядочить серию локальных коммитов | **`rebase -i`** |
| Временно убрать правки | **`stash`** |
| Откатить ветку локально (ещё не push) | **`reset --soft/mixed`** |
| Откатить опубликованный коммит | **`revert`** |
| Найти «потерянный» SHA | **`reflog`** |

## Практическое правило

- Перед **`reset --hard`** — коммит, stash или запишите SHA (`git rev-parse HEAD`).
- После amend/rebase на уже запушенной ветке — только **`--force-with-lease`**
  и только по договорённости (уровень 5).
""",
    5: """# Распределённая работа и платформы

Git **распределённый**: у каждого разработчика — полная копия истории.
**Remote** — именованная ссылка на репозиторий-colleague или сервер. Этот
уровень — clone, fetch, pull, push, bare-репозитории, fork-and-pull и обзор
GitHub / GitLab.

До этого вы работали в основном «у себя дома». Теперь появляется **общий
источник правды** на сервере и правила, как не затереть чужую работу. Спокойный
ритм `fetch` → посмотреть → `merge` или `rebase` → `push` экономит нервы в
реальной команде.

## Remotes как «закладки»

**Remote** (обычно **`origin`**) — URL + набор **remote-tracking branches**
(`origin/main`). Они обновляются при **`fetch`**, но **не меняют** ваши локальные
ветки автоматически.

```bash
git remote -v
git remote add upstream https://example.com/upstream.git
git branch -vv
```

## Модели совместной работы

### Централизованная (shared repository)

Все пушат в **один** remote; часто `main` защищена, работа — в feature-ветках
+ PR/MR. Типично для продуктовых команд.

### Интегратор (integrator / lieutenant)

Несколько **maintainer**-ов; вклад через PR, merge делает integrator. Fork-and-pull
(ниже) — частный случай для open source.

### Fork-and-pull

Внешний contributor **форкает** репозиторий, пушит в **свой fork**, открывает
PR в **upstream**. У вас: `origin` = fork, `upstream` = оригинал.

```bash
git remote add upstream git@github.com:org/project.git
git fetch upstream
git merge upstream/main
```

## Основные команды

### `git clone`

**Определение:** копирует репозиторий: объекты, ветки, настраивает `origin`,
создаёт рабочее дерево.

```bash
git clone https://github.com/org/project.git
git clone git@github.com:org/project.git
```

Задача **`clone_local`**.

### `git fetch`

**Определение:** скачивает объекты и обновляет remote-tracking refs **без**
слияния в текущую ветку.

```bash
git fetch origin
git fetch --all --prune
```

- **`--prune`** — удалить локальные `origin/feature`, если на сервере ветку
  уже удалили.

### `git pull`

**Определение:** **`fetch` + интеграция** в текущую ветку.

По умолчанию — **merge**. Альтернатива:

```bash
git pull --rebase
```

**`pull --rebase`** — ваши локальные коммиты «переигрываются» поверх свежего
remote; история линейнее, но при конфликтах нужен `--continue`.

Задачи **`fetch_merge`**, **`pull_rebase`**.

### `git push`

**Определение:** отправляет коммиты на remote и обновляет ветку там.

```bash
git push origin main
git push -u origin feature-x    # установить upstream
git push --force-with-lease     # осторожный force после rebase
```

**`--force-with-lease`** — перезапись только если remote не изменился с момента
вашего fetch. Задачи **`push_first`**, **`push_conflict`**.

## Bare-репозитории

**Bare** — репозиторий **без рабочего каталога**: только `.git`-данные.
Используется как **серверная** «источник правды»; push идёт **в bare**, не в
чужой рабочий каталог (иначе рассинхрон).

```bash
git clone --bare project.git
```

Разработчики работают в **non-bare** клонах; публикация — в bare / на платформе.

## Защищённые ветки

На GitHub/GitLab **protected branch** (например `main`):

- Запрет прямого push или force-push.
- Обязательный PR/MR и code review.
- Обязательный зелёный CI (commit status).

Локально Git не знает о защите — правила enforced на сервере.

## SSH-аутентификация

Вместо HTTPS с токеном — пара ключей:

```bash
ssh-keygen -t ed25519 -C "you@example.com"
# публичный ключ → Settings → SSH Keys на платформе
git clone git@github.com:org/repo.git
```

Удобно для частых push без ввода пароля.

## Офлайн: `git bundle`

Перенос репозитория **без сети** — один файл-бандл:

```bash
git bundle create repo.bundle --all
git clone repo.bundle project-copy
```

Задача **`create_offline_bundle`**.

## GitHub и GitLab: сравнение

| Понятие | GitHub | GitLab |
| --- | --- | --- |
| Запрос на слияние | Pull Request (PR) | Merge Request (MR) |
| Задачи | Issues | Issues |
| CI/CD | GitHub Actions (`.github/workflows/`) | GitLab CI (`.gitlab-ci.yml`) |
| Статический сайт | GitHub Pages | GitLab Pages |
| Wiki / Docs | Wiki, README | Wiki, GitLab Pages |
| Container registry | ghcr.io | GitLab Container Registry |
| Self-hosted | GitHub Enterprise | GitLab CE/EE |
| Markdown задач | `#42`, `Fixes #42` | `#42`, `Closes #42`, GLFM |
| Ссылки в MR | `#issue`, `@user` | `#issue`, `@user`, `!123` (MR) |
| Runners | GitHub-hosted / self-hosted | GitLab Runners (shared/specific) |
| API | REST + GraphQL | REST API |
| Fork workflow | Fork + PR | Fork + MR |
| Protected branches | Branch protection rules | Protected branches |
| Releases | Releases + assets | Releases + assets |
| Code owners | CODEOWNERS | CODEOWNERS |

Платформы **не заменяют** Git — они добавляют UI, права, CI и интеграции
**поверх** remotes.

## Безопасный цикл

```
fetch → review (log, diff) → merge или rebase → test → push
```

- Подтягивайте перед push.
- Конфликты решайте **локально**.
- Не пушьте секреты; используйте `.gitignore` (уровень 7).

## Практика

- **`git remote -v`** — куда смотрят remotes.
- **`git branch -vv`** — tracking и отставание/опережение (`[ahead 2, behind 1]`).
- Для open source: **`upstream`** + **`origin`** — два remotes, один fork.
""",
    6: """# Устройство Git и продвинутые приёмы

Под капотом Git — **объектная база** (blob, tree, commit, tag), ссылки (refs)
и мощные инструменты диагностики: bisect, blame, диапазоны log, notes,
worktree, submodule, хуки и подписи.

Знание внутренностей не обязательно для первых коммитов, но сильно помогает,
когда «что-то странное» в истории или статусе. Не нужно учить всё наизусть —
достаточно понимать, **какие объекты** стоят за привычными командами.

## Объектная модель

Четыре типа объектов — кратко в таблице:

| Тип | Содержимое |
| --- | --- |
| **blob** | Содержимое файла (без имени) |
| **tree** | Каталог: имена → SHA blob/tree |
| **commit** | Tree + родители + автор + сообщение + timestamp |
| **tag** | Указатель на commit (annotated tag — отдельный объект) |

Каждый объект идентифицируется **SHA-1** (или SHA-256 в новых репозиториях).
Целостность — по хешу содержимого.

```
commit ──► tree ──► blob (README)
              └──► tree (src/) ──► blob (main.py)
```

## Tree-ish и синтаксис ссылок

**Tree-ish** — anything Git может разрешить в tree/commit:

| Запись | Пример | Смысл |
| --- | --- | --- |
| Ветка | `main` | Tip ветки |
| HEAD | `HEAD`, `HEAD~3` | Текущий коммит, 3 коммита назад |
| Родитель | `main^`, `main~1` | Первый родитель |
| N-й родитель merge | `main^2` | Второй родитель |
| Tree из commit | `main^{tree}` | Tree-объект коммита |
| Tag | `v1.0` | Коммит, на который указывает тег |

```bash
git rev-parse main~3
git rev-parse HEAD^{tree}
```

Задачи **`rev_parse_head_sha`**, **`save_symbolic_head`**.

## Просмотр объектов

### `git cat-file`

```bash
git cat-file -p HEAD          # pretty-print объекта
git cat-file -t abc1234       # тип: commit, tree, blob, tag
git cat-file -s abc1234       # размер
```

### `git ls-tree`

**Определение:** содержимое **tree** (файлы и режимы в снимке).

```bash
git ls-tree HEAD
git ls-tree -r HEAD           # рекурсивно
```

Задачи **`inspect_objects`**, **`tree_list_root`**.

### Низкоуровневое создание объектов

Как в задаче **`write_git_blob`**:

```bash
echo "hello" | git hash-object -w --stdin
git update-index --add --cacheinfo 100644 <sha> file.txt
```

Это «plumbing» — редко вручную, но объясняет, что **`git add`** строит tree
через индекс.

## `git bisect`

**Двоичный поиск** первого «плохого» коммита:

```bash
git bisect start
git bisect bad HEAD
git bisect good v1.0.0
# тестируете, помечаете good/bad, пока Git не найдёт виновника
git bisect reset
```

### `git bisect run`

Автоматизация: скрипт с кодом выхода 0 = good, иначе bad.

```bash
git bisect run ./test.sh
```

Задача **`find_bisect`**.

## `git blame`

Построчно: **какой коммит** последним трогал строку.

```bash
git blame -L 10,20 src/app.py
git blame -w file.py    # игнорировать whitespace
```

## Диапазоны в `git log`

| Синтаксис | Смысл |
| --- | --- |
| **`A..B`** | Коммиты достижимые из **B**, но **не** из **A** («что в B после A») |
| **`A...B`** | Коммиты, уникальные для **каждой** ветки от **merge-base(A,B)** |

```bash
git log main..feature-x
git log main...feature-x
```

Задачи **`log_double_dot_range`**, **`triple_dot_log_range`**.

## Pickaxe: `git log -S`

Коммиты, где **менялось количество вхождений** строки (добавление/удаление
функции, константы):

```bash
git log -S "def authenticate" --oneline
git log -G "regex-pattern"
```

Задача **`pickaxe_log_search`**.

## `git notes`

**Notes** — метаданные **рядом** с коммитом, не меняя сам commit (review URL,
ticket id):

```bash
git notes add -m "Reviewed-by: Anna" abc1234
git log --show-notes
```

Задача **`attach_git_note`**.

## Worktree и submodule

### Worktree

Вторая рабочая копия **того же** `.git`:

```bash
git worktree add ../hotfix hotfix-branch
```

Задача **`worktree`**.

### Submodule

Вложенный репозиторий, зафиксированный **конкретным SHA** в родителе:

```bash
git submodule add https://example.com/lib.git vendor/lib
git submodule update --init --recursive
```

## Патчи: format-patch и am

```bash
git format-patch -1 HEAD              # один .patch файл
git format-patch origin/main..HEAD    # серия коммитов
git am 0001-*.patch                   # применить серию
```

Полезно для email-review и офлайн-обмена (уровень 9).

## Обслуживание: fsck и gc

```bash
git fsck --full           # проверка целостности объектов
git gc --prune=now        # сборка мусора, упаковка
```

## Хуки

Скрипты в **`.git/hooks/`** (или через **`core.hooksPath`**):

| Хук | Когда |
| --- | --- |
| **pre-commit** | До создания коммита (lint, тесты) |
| **commit-msg** | Проверка формата сообщения |
| **pre-push** | Перед push |

Пример **`commit-msg`**: требовать issue id. Задача **`custom_aliases_hooks`**.

## Sign-off и GPG

- **`git commit -s`** — строка **`Signed-off-by:`** (согласие с DCO проекта).
- **`git commit -S`** — **GPG-подпись** коммита (verified на GitHub/GitLab).

```bash
git commit -s -m "Fix login redirect"
git tag -s v1.0 -m "Signed release"
```

## `git filter-repo`

Если в историю попал **секрет** или огромный бинарник — нужно **переписать
все коммиты**. Современный инструмент — **`git filter-repo`** (рекомендуется
вместо устаревшего **`filter-branch`**):

```bash
git filter-repo --path secrets.env --invert-paths
```

Требует **force-push** и согласования с командой. Задача **`filter_branch`**
знакомит с идеей переписывания истории.

## Алиасы

```bash
git config --global alias.st "status -sb"
git config --global alias.lg "log --graph --oneline --all"
```

## Связь с задачами уровня

| Задача | Тема |
| --- | --- |
| **`find_bisect`** | bisect / bisect run |
| **`reflog_recovery`** | reflog |
| **`inspect_objects`** | cat-file, ls-tree |
| **`write_git_blob`** | hash-object, plumbing |
| **`log_double_dot_range`** | A..B |
| **`triple_dot_log_range`** | A...B |
| **`pickaxe_log_search`** | log -S |
| **`attach_git_note`** | git notes |
| **`worktree`** | parallel worktrees |
| **`custom_aliases_hooks`** | hooks и alias |
""",
    7: """# Гигиена репозитория

Не всё в папке проекта должно попадать в Git: логи, артефакты сборки,
зависимости (`node_modules/`), локальные секреты (`.env`). Этот уровень —
**.gitignore**, снятие с отслеживания, **`.gitkeep`** и **`git clean`**.

Аккуратный репозиторий — это уважение к будущему себе и к команде: меньше
случайных коммитов с мусором, быстрее клон, проще ревью. Потратьте десять минут
на ignore в начале проекта — сэкономите часы потом.

## Зачем гигиена

Типичные проблемы и к чему они приводят:

| Проблема | Последствие |
| --- | --- |
| Секрет в истории | Компрометация ключей (удаление из истории — больно) |
| `node_modules/` в repo | Огромный clone, merge-ад |
| `.DS_Store`, `*.log` | Шум в `git status` и diff |

Правило: **настройте ignore до первого коммита** мусора.

## `.gitignore`

**Определение:** текстовый файл со **шаблонами** имён; Git **не предлагает**
и не отслеживает подходящие пути (untracked).

### Синтаксис

```gitignore
# Комментарий

# Конкретный файл
config.local.ini

# Все .log в любом каталоге
*.log

# Каталог целиком
node_modules/
dist/

# Только в корне репозитория
/TODO.txt

# Исключение из игнора
logs/*
!logs/.gitkeep

# Вложенность
**/temp/
```

| Шаблон | Эффект |
| --- | --- |
| `*.log` | Все файлы с расширением `.log` |
| `build/` | Каталог `build` на любом уровне |
| `/build/` | Только `build` в **корне** |
| `!important.log` | **Не игнорировать** (после более широкого правила) |

Задачи **`setup_ignore`**, **`ignore_node_modules`**, **`ignore_exceptions`**.

### Где хранить правила

- **`.gitignore`** в корне — основной файл проекта.
- **`.git/info/exclude`** — личные локальные правила (не коммитятся).
- **Глобальный** `~/.config/git/ignore` — для всех ваших репозиториев.

### Готовые шаблоны

Сервисы вроде [gitignore.io](https://www.toptal.com/developers/gitignore)
генерируют стартовый `.gitignore` по стеку (Python, Node, IDE).

## Уже отслеживаемый файл

**`.gitignore` не действует** на файлы, уже в индексе. Нужно **убрать из
индекса**, оставив на диске:

```bash
git rm --cached secrets.env
git rm -r --cached node_modules/
git commit -m "Stop tracking generated files"
```

Задача **`untrack_cached`**.

## `.gitkeep`

Git **не хранит пустые каталоги** — только файлы. Чтобы сохранить структуру
(`uploads/`, `logs/`), кладут **пустой маркер**:

```bash
touch uploads/.gitkeep
git add uploads/.gitkeep
```

**`.gitkeep`** — не официальная команда Git, а **соглашение** сообщества.
Альтернатива — **`README.md`** в каталоге с пояснением назначения.

Задача **`keep_empty_dir`**.

## `git clean`

Удаление **untracked** файлов из рабочего каталога:

```bash
git clean -n              # dry-run: что будет удалено
git clean -f              # удалить untracked файлы
git clean -fd             # + untracked каталоги
git clean -fdx            # + игнорируемые (осторожно!)
```

**Всегда** начинайте с **`-n`**. Задача **`clean_untracked`**.

## Сводная таблица

| Инструмент | Назначение |
| --- | --- |
| **`.gitignore`** | Не видеть мусор в status |
| **`git rm --cached`** | Перестать отслеживать, файл остаётся на диске |
| **`.gitkeep`** | Зафиксировать пустой каталог |
| **`git clean -n/-f`** | Убрать лишние untracked файлы |

## Практический чеклист

1. Создать `.gitignore` **до** `git add .` на новом проекте.
2. Никогда не коммитить `.env`, ключи, пароли.
3. Если секрет уже в истории — ротация ключей + **`filter-repo`** (уровень 6).
4. Периодически **`git status`** — нет ли случайных больших файлов.
""",
    8: """# Теги и релизы

**Тег** — именованная **закладка на коммит**. В отличие от ветки, тег **не
двигается** при новых коммитах. Теги фиксируют релизы: `v1.0.0`, `v2.4.1`.

Пользователям и коллегам важно не только «код в main», но и **именованная
версия**, к которой можно вернуться. Теги — мост между историей Git и релизами
на GitHub/GitLab.

## Легковесные и аннотированные теги

| Тип | Суть | Когда использовать |
| --- | --- | --- |
| **Lightweight** | Просто ref на commit | Личные закладки, временные метки |
| **Annotated** | Объект tag: автор, дата, сообщение, опционально GPG | **Релизы**, публикация |

### Lightweight

```bash
git tag v1.0-lw
```

### Annotated

```bash
git tag -a v1.0.0 -m "Первый стабильный релиз"
git tag -s v1.0.0 -m "Подписанный релиз"   # GPG
```

Задачи **`create_lightweight_tag`**, **`create_tag`**.

## Просмотр

```bash
git tag
git tag -l "v1.*"
git show v1.0.0
```

Задачи **`show_tag`**, **`tag_old_commit`** (тег на прошлый SHA):

```bash
git tag -a v1.2.0 abc1234 -m "Патч-релиз"
```

## Push тегов

**`git push` по умолчанию не отправляет теги.**

```bash
git push origin v1.0.0
git push origin --tags       # все локальные теги
```

Задача **`push_tags`**.

## Удаление

```bash
git tag -d v1.0.0
git push origin --delete v1.0.0
```

**Не переставляйте** опубликованные теги: если `v1.0.0` уже скачали —
создайте **`v1.0.1`**, а не «передвиньте» `v1.0.0`.

## Semantic Versioning (SemVer)

Формат **`MAJOR.MINOR.PATCH`** (`X.Y.Z`):

| Часть | Когда увеличивать |
| --- | --- |
| **MAJOR** | Несовместимые изменения API |
| **MINOR** | Новая функциональность, обратно совместимо |
| **PATCH** | Исправления багов |

Примеры: `1.0.0` — первый релиз; `1.0.1` — hotfix; `1.1.0` — новая фича;
`2.0.0` — breaking change.

### `git describe`

Человекочитаемое имя от ближайшего тега:

```bash
git describe --tags
# v1.2.0-5-gabc1234  → 5 коммитов после v1.2.0, g = git, abc1234 = SHA
git describe --tags --always
```

Связано с задачей **`semantic_describe`** (уровень 9).

## Releases на GitHub и GitLab

**Release** на платформе — UI поверх **annotated tag**:

- Changelog, бинарные артефакты, docker-образы.
- Автогенерация notes из PR/MR.
- Связь с CI (сборка по tag push).

Типичный поток:

```bash
git tag -a v1.0.0 -m "Release 1.0.0"
git push origin v1.0.0
# на платформе: Create Release from tag v1.0.0
```

## Тег vs ветка релиза

| | Тег | Ветка `release/x.y` |
| --- | --- | --- |
| Двигается | Нет | Да (hotfix на линии) |
| Стабильность | Иммутабельная метка | Живая линия стабилизации |

Многие команды: feature → `main` → tag `vX.Y.Z`; долгие релизы — отдельная
ветка, финал — tag.

## Практика

- Релизы — **annotated** + сообщение.
- SemVer + **`git push origin vX.Y.Z`** + Release на платформе.
- Локально проверяйте: **`git show v1.0.0`**, **`git log v1.0.0 -1`**.
""",
    9: """# Профессиональные практики и CI

Девятый уровень связывает Git с **реальной командной работой**: интерфейс
репозитория на GitHub/GitLab, issues и PR/MR, CI/CD, Pages, API и переносимые
патчи. В песочнице GitPlayground сеть отключена, но **команды и файлы** те же —
они готовят к production-процессам.

Здесь много «обвязки» вокруг Git: задачи, ревью, пайплайны, публикация документации.
Не обязательно запоминать всё сразу — используйте уровень как карту: что искать
на платформе, когда в терминале уже сделали свою часть.

## Репозиторий на платформе и Git

Веб-UI — **обёртка** над объектами Git и метаданными платформы. Многие кнопки
имеют точный локальный аналог — сверяйтесь с таблицей:

| Действие в UI | Git / файлы |
| --- | --- |
| Create repository | `git init` + `git remote add` + `git push -u` |
| Upload file | `echo ... > file` + `git add` + `git commit` + `git push` |
| Create branch | `git switch -c feature-x` + `git push -u origin feature-x` |
| Open Pull/Merge Request | ветка уже запушена; merge = `git merge` на сервере |
| Fork | `git clone` своего fork + `remote add upstream` |
| Releases | `git tag -a` + `git push origin vX.Y.Z` |
| Delete branch | `git push origin --delete feature-x` |
| View commit | `git show SHA` |
| Blame | `git blame file` |

### Watch, Star, Fork

- **Watch** — уведомления о активности репозитория.
- **Star** — закладка / метрика популярности (не влияет на Git-объекты).
- **Fork** — **копия репозитория** под вашим аккаунтом; свой `origin`, PR в upstream.

Задача **`readme_first`** — первый коммит с **README.md**: «визитка» repo на
платформе; без README пустой проект выглядит незавершённым.

```bash
echo "# My Project" > README.md
git add README.md
git commit -m "Add README"
```

## Issues: задачи и обсуждение

**Issue** — ticket с номером `#N`: баг, фича, вопрос.

| Элемент | Назначение |
| --- | --- |
| **Title / body** | Описание проблемы |
| **Labels** | Категории: `bug`, `enhancement`, `docs` |
| **Milestone** | Группа issues к версии / спринту |
| **Assignee** | Ответственный |
| **Linked PR/MR** | Связь с кодом |

### Автозакрытие из коммита или MR

В **сообщении коммита** или **описании PR/MR** (при merge в default branch):

| Ключевое слово | Платформа |
| --- | --- |
| **`Fixes #42`**, **`Closes #42`**, **`Resolves #42`** | GitHub |
| **`Closes #42`**, **`Fixes #42`** | GitLab |

```bash
git commit -m "Fix null pointer in parser

Closes #42"
```

Задачи **`issue_close_message`**, **`closes_issue_gitlab`**.

На GitLab в markdown используют **GLFM** (GitLab Flavored Markdown):

- **`@username`** — упоминание пользователя
- **`#123`** — issue
- **`!456`** — merge request
- **`~label`**, **`%milestone`**

Задача **`gitlab_md_issue_ref`**.

## Pull Request / Merge Request

**PR (GitHub)** / **MR (GitLab)** — запрос **влить ветку** в целевую (обычно
`main`) с обсуждением и review.

### Типичный workflow

```
1. git switch -c feature/issue-42
2. коммиты...
3. git push -u origin feature/issue-42
4. Open PR/MR в UI
5. Code review, CI green
6. Merge (merge / squash / rebase — политика команды)
7. git switch main && git pull
8. git branch -d feature/issue-42
```

Задача **`mr_feature_branch`** — feature-ветка под MR.

### Code review

Reviewers смотрят **diff** (тот же `git diff main...feature`), оставляют
comments на строках. Локально можно подтянуть PR:

```bash
git fetch origin pull/123/head:pr-123
git switch pr-123
```

### Task lists в markdown

Чеклисты в описании issue/PR:

```markdown
- [x] Добавить валидацию
- [ ] Написать тесты
```

Платформа отображает прогресс; не путать с Git-объектами.

### Squash merge на платформе

Все коммиты PR → **один** на `main`. История feature «схлопнута»; на локальной
ветке история может отличаться — после merge обновите `main`.

## Организации, команды, права

| Уровень | GitHub | GitLab |
| --- | --- | --- |
| Организация | Organization | Group |
| Команда внутри | Team | Subgroup / Team |
| Роль read/write/admin | Repository permissions | Guest / Reporter / Developer / Maintainer / Owner |

**Protected branches** + **required reviews** + **required CI** — стандарт для
`main`. Прямой push в `main` запрещён; только через merged PR/MR.

## CI/CD и commit status

### GitHub Actions

Workflow в **`.github/workflows/ci.yml`**:

```yaml
name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: make test
```

### GitLab CI

Файл **`.gitlab-ci.yml`** в корне:

```yaml
stages:
  - test

unit:
  stage: test
  script:
    - pytest
```

**Runners** выполняют job: shared (платформа) или self-hosted у вас.

Задача **`add_gitlab_ci_yaml`**.

### Commit status / pipeline badge

CI публикует **status** на commit SHA (`pending` / `success` / `failure`).
PR/MR блокируется, пока status не green — **merge gating**.

```
feature ──► commits ──► push ──► CI runs on SHA ──► status ──► merge allowed?
```

## GitHub Pages и Jekyll

**GitHub Pages** публикует статический сайт из ветки или `/docs`:

| Источник | Ветка / путь |
| --- | --- |
| Classic | ветка **`gh-pages`**, корень или `/docs` |
| Jekyll | `_config.yml`, `_posts/`, layouts |

Создание ветки Pages:

```bash
git switch --orphan gh-pages
git commit --allow-empty -m "Init gh-pages"
git push -u origin gh-pages
```

Задача **`gh_pages_branch`**.

### Jekyll: пост с front matter

Файл **`_posts/YYYY-MM-DD-title.md`**:

```markdown
---
layout: post
title: "Первый пост"
date: 2026-07-10 12:00:00 +0400
categories: blog
---

Текст поста в Markdown.
```

YAML между `---` — **front matter**; Jekyll генерирует HTML.

Задача **`jekyll_post_front_matter`**.

**GitLab Pages** — аналог через CI job `pages` и artifact `public/`.

## Модели workflow (сводная таблица)

| Модель | Суть | Когда |
| --- | --- | --- |
| **Trunk-based** | Короткие ветки (<1–2 дня), частый merge в `main`, feature flags | Сильный CI/CD, DevOps-культура |
| **Feature branches** | Ветка на задачу → PR/MR → merge | Большинство продуктовых команд |
| **Fork-and-pull** | Fork + PR в upstream | Open source, внешние contributors |
| **Gitflow** | `develop`, `release/*`, `hotfix/*`, `main` | Плановые релизы, legacy-процессы |

Gitflow тяжелее trunk-based; многие команды упростились до **feature branch +
main + tags**.

## Патches: format-patch, bundle, am

Перенос изменений **без прямого push** или **офлайн**:

```bash
# один коммит → файл .patch
git format-patch -1 HEAD

# серия коммитов поверх main
git format-patch origin/main..HEAD

# применить
git am 0001-*.patch

# офлайн весь repo
git bundle create full.bundle --all
git clone full.bundle my-copy
```

Задача **`export_format_patch`**.

Patch из URL PR (концептуально): скачать `.patch` / `git fetch origin
pull/N/head` — тот же diff, другой транспорт.

## Качество коммитов

### Sign-off (DCO)

```bash
git commit -s -m "Add export module"
# Signed-off-by: Name <email@example.com>
```

Задача **`commit_signoff`**.

### Conventional commits (кратко)

Префиксы: `feat:`, `fix:`, `docs:`, `chore:` — для changelog и семантического
версионирования.

### `git describe` и SemVer

```bash
git describe --tags --always
# v1.2.0-3-gabc1234
```

Задача **`semantic_describe`**.

## Переименование через Git

**`git mv`** сохраняет историю лучше, чем ручное rename + add:

```bash
git mv old_name.py src/new_name.py
git commit -m "Rename module for clarity"
```

Задача **`git_mv_rename`**.

## REST API (концептуально)

Платформы expose **HTTP API** поверх Git-данных и метаданным (issues, PR).

| Тема | Суть |
| --- | --- |
| **Hypermedia / pagination** | `Link` header, `page`, `per_page` |
| **Rate limit** | Лимит запросов в час; заголовки `X-RateLimit-*` |
| **Auth** | PAT (token), OAuth apps со **scopes** (`repo`, `read:user`) |
| **Идempotency** | Повтор POST может дублировать — используйте ожидаемые id |

Примеры **концептуальных** endpoint (не вызывайте в песочнице без токена):

```
GET /repos/{owner}/{repo}/commits
GET /projects/{id}/merge_requests
POST /repos/{owner}/{repo}/issues
```

Scopes ограничивают blast radius токена.

## Git Data API / plumbing (локально)

Низкоуровневые операции, которые API имитирует на сервере — у вас локально:

```bash
# blob
echo "content" | git hash-object -w --stdin

# tree (обычно через index)
git write-tree

# commit
tree=$(git write-tree)
git commit-tree $tree -p HEAD -m "Message"

# tag
git tag -a v1.0.0 -m "Release"
```

Задача **`write_git_blob`** тренирует **`hash-object -w`**: понимание, что
**файл в Git = именованный указатель на blob**.

## Интеграция CI и веток

```
Developer          Platform              CI
    │                  │                   │
    ├── push branch ──►│                   │
    │                  ├── webhook/trigger ►│
    │                  │                   ├── test job
    │                  │◄── status SHA ────┤
    │                  │                   │
    ├── open PR/MR ───►│ (merge blocked    │
    │                  │  until green)     │
    │◄── review ───────┤                   │
    ├── merge ────────►│                   │
```

## GitHub vs GitLab: практики в одном месте

| Задача | GitHub | GitLab |
| --- | --- | --- |
| Закрыть issue из merge | `Fixes #N` в PR | `Closes #N` в MR |
| CI config | `.github/workflows/*.yml` | `.gitlab-ci.yml` |
| Статический сайт | Pages из `gh-pages` / Actions | Pages job |
| Ссылка на MR | PR `#N` | `!N` в GLFM |
| Fork workflow | Fork + PR | Fork + MR |

## Практика в песочнице GitPlayground

Сеть недоступна, но отрабатываются **те же приёмы**:

| Задача | Что тренируется |
| --- | --- |
| **`readme_first`** | README как первый коммит |
| **`issue_close_message`** | `Fixes #N` / `Closes #N` в сообщении |
| **`gh_pages_branch`** | ветка `gh-pages` |
| **`jekyll_post_front_matter`** | `_posts/` + YAML front matter |
| **`write_git_blob`** | `git hash-object -w` |
| **`mr_feature_branch`** | feature-ветка под MR |
| **`add_gitlab_ci_yaml`** | `.gitlab-ci.yml` |
| **`closes_issue_gitlab`** | автозакрытие issue GitLab-стилем |
| **`gitlab_md_issue_ref`** | GLFM: `@user`, `#issue`, `!MR` |
| **`export_format_patch`** | `git format-patch` |
| **`commit_signoff`** | `git commit -s` |
| **`semantic_describe`** | `git describe --tags` |
| **`git_mv_rename`** | `git mv` + commit |

Пример сессии, объединяющей несколько тем:

```bash
git switch -c feature/docs-readme
echo "# Project" > README.md
git add README.md
git commit -s -m "Add README for repository landing

Closes #1"

git switch --orphan gh-pages
echo "pages" > index.html
git add index.html
git commit -m "Init GitHub Pages"

git switch main
git format-patch -1 HEAD
git describe --tags --always
echo "test" | git hash-object -w --stdin
```

## Чеклист перед merge в main

- [ ] Ветка актуальна относительно `main` (rebase/merge).
- [ ] CI / локальные тесты зелёные.
- [ ] Сообщения коммитов осмысленны; issue linked при необходимости.
- [ ] Нет секретов и мусорных файлов (`.gitignore`).
- [ ] Review approved (на платформе).
- [ ] После merge — удалить feature-ветку локально и на remote.

Профессиональная работа с Git — это **не только команды**, а согласованный
процесс: ветка → коммиты → push → review → CI → merge → tag/release → Pages.
GitPlayground даёт безопасную песочницу отработать каждый шаг локально.
""",
}

LEVEL_SECTION_HINTS = {
    1: (
        "Работай по циклу: status → add → commit → log.",
        "Буферная зона — ключ к управлению изменениями перед коммитом.",
    ),
    2: (
        "Ветка — это быстрый способ переключить контекст без клонирования каталога.",
        "Для срочных задач создавай отдельную ветку, а затем делай слияние.",
    ),
    3: (
        "Конфликт слияния решается вручную, потом изменения нужно повторно добавить в буфер.",
        "После merge проверь граф истории и убедись, что коммиты связаны ожидаемо.",
    ),
    4: (
        "Используй rebase -i чтобы собрать историю в читаемую последовательность.",
        "Перед reset --hard создай страховочную ветку или пометь текущий SHA.",
    ),
    5: (
        "Проверяй remotes через git remote -v и отслеживай ветки через git branch -vv.",
        "Безопасный цикл: fetch -> review -> merge/rebase -> push.",
    ),
    6: (
        "При поиске регрессии используй бинарный подход bisect, а не линейный перебор.",
        "Для контроля качества подключай pre-commit и commit-msg хуки.",
    ),
    7: (
        "Добавь шаблоны в .gitignore до первого коммита мусорных файлов.",
        "Для пустой папки используй .gitkeep или README внутри каталога.",
    ),
    8: (
        "Для релизов предпочитай аннотированные теги с осмысленным сообщением.",
        "Не забудь git push origin --tags после создания тегов локально.",
    ),
    9: (
        "Перед merge убедись, что CI зелёный и сообщение коммита ссылается на issue при необходимости.",
        "format-patch и describe помогают переносить и именовать изменения без путаницы.",
    ),
}

LEVEL_DIAGRAMS = {
    1: "flowchart LR\n  worktree[Working tree] --> index[Index]\n  index --> commit[Commit]\n  commit --> log[git log]",
    2: "graph LR\n  main[main] --> C1\n  C1 --> C2\n  C1 --> Fx1[feature-x]\n  Fx1 --> Fx2\n  main -. checkout .-> Fx2",
    3: "graph LR\n  M1 --> M2\n  M2 --> F1\n  M2 --> M3\n  F1 --> F2\n  M3 --> Merge((merge))\n  F2 --> Merge",
    4: "flowchart TD\n  A[commit A] --> B[commit B]\n  B --> C[commit C]\n  C --> D[commit D]\n  D --> R[rebase -i]\n  R --> C2[clean history]",
    5: "graph LR\n  Dev[local repo] -- push --> Origin[(origin)]\n  Origin -- fetch --> Dev\n  Origin -- fetch --> UpstreamClone[teammate clone]\n  Dev -- fetch upstream --> Upstream[(upstream)]",
    6: "flowchart LR\n  Bad[regression] --> Bisect[git bisect]\n  Bisect --> Good[good commit]\n  Bisect --> Bad2[bad commit]\n  Good --> Fix[fix + hook]\n  Fix --> Objects[cat-file / ls-tree]",
    7: "flowchart TB\n  A[Рабочая папка проекта] --> B{Файл/папка нужен в истории?}\n  B -- Нет --> C[Добавить шаблон в .gitignore]\n  B -- Да --> D{Папка пустая?}\n  D -- Да --> E[Добавить .gitkeep]\n  D -- Нет --> F[Коммитить как обычно]\n  C --> G[Чистый git status]\n  E --> G\n  F --> G",
    8: "graph LR\n    C1((C1)) --> C2((C2))\n    C2 --> C3((C3))\n    T1[tag: v0.1] --- C1\n    T2[tag: v1.0] --- C3\n    C3 --> C4((C4))\n    main[Ветка: main] --- C4\n    classDef tagStyle fill:#fff9c4,stroke:#fbc02d,color:#000\n    class T1,T2 tagStyle",
    9: "flowchart LR\n  Dev[feature branch] --> MR[Merge Request]\n  MR --> CI[CI pipeline]\n  CI --> Main[main]\n  Main --> Tag[annotated tag]\n  Tag --> Pages[GitHub/GitLab Pages]",
}
