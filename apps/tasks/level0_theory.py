"""Level 0 theory block: terminal basics and what Git is."""

LEVEL_0_THEORY = """# Терминал и знакомство с Git

Перед первыми коммитами полезно понять **два инструмента**: окно **терминала** (командная
строка) и сам **Git**. На этом уровне вы потренируетесь работать в терминале песочницы —
без Git-команд. С ними начнётся уровень 1.

> **Главная идея уровня:** терминал — это способ **отдавать компьютеру короткие текстовые
> команды**. Файлы и папки при этом реальные; результат виден сразу.

## Что такое терминал

**Терминал** (командная строка, shell) — текстовый интерфейс к операционной системе.
Вместо кликов по иконкам вы **печатаете команду** и нажимаете Enter. Компьютер выполняет
её и показывает ответ текстом.

В GitPlayground терминал **безопасный**: команды выполняются только внутри вашей учебной
папки. Произвольный shell (например `bash -c` или `python -c`) недоступен — это защита
от случайных и опасных действий.

### Как читать приглашение

Строка вроде `~/repo $` означает: «вы в учебной папке, вводите команду». После Enter
появится вывод команды и снова приглашение.

### Сводная таблица команд

| Команда | Зачем |
| --- | --- |
| `pwd` | Показать текущую папку |
| `ls` | Список файлов и каталогов |
| `whoami` | Имя пользователя в песочнице |
| `mkdir` / `mkdir -p` | Создать каталог |
| `touch` | Создать пустой файл |
| `echo текст` | Вывести текст на экран |
| `echo текст > файл` | Записать текст в файл (перезапись) |
| `echo текст >> файл` | Дописать текст в конец файла |
| `type nul > файл` | Создать пустой файл (привычный Windows-синтаксис) |
| `cat` | Показать содержимое файла |
| `head` / `tail` | Первые / последние строки файла |
| `wc -l` | Посчитать строки в файле |
| `cp` | Копировать файл |
| `mv` | Переместить или переименовать |
| `rm` | Удалить файл |
| `find` | Найти файлы в каталоге |
| `nano` / `edit` | Открыть встроенный редактор (Ctrl+S — сохранить, Ctrl+X — выйти) |
| `clear` | Очистить экран терминала |

Ниже — разбор каждой команды подробнее. Символы `>` и `>>` — **перенаправление вывода**
в файл; в песочнице их обрабатывает не произвольный shell, а учебный интерпретатор команд.

## `pwd` — текущая папка

`pwd` (print working directory) печатает путь к каталогу, в котором вы «стоите» сейчас.
В песочнице это почти всегда `~/repo` — корень учебной папки задачи.

Перед созданием файлов и каталогов полезно убедиться, что вы не «ушли» мысленно в другое
место: все относительные пути (`practice/notes.txt`) считаются от текущего каталога.

Пример:

```bash
pwd
# ~/repo
```

## `ls` — список файлов

`ls` показывает имена файлов и каталогов. Можно указать путь: `ls practice`.
В GitPlayground длинные флаги вроде `-la` принимаются, но вывод упрощённый: важны имена,
а не права доступа Unix.

Типичный сценарий: сделали `mkdir` / `touch` → `ls` → убедились, что объекты появились.

## `whoami` — кто вы в песочнице

Команда печатает учебное имя пользователя. В обычной ОС это логин учётки; здесь —
фиксированный идентификатор песочницы (`gitplayground`). Нужна, чтобы привыкнуть:
терминал отвечает на короткие запросы о среде, а не только о файлах.

## `mkdir` — создать каталог

`mkdir practice` создаёт одну папку. `mkdir -p practice/nested` создаёт цепочку каталогов
и **не падает**, если какой-то уровень уже существует.

Без каталога нельзя положить файл по пути `practice/notes.txt` — сначала папка, потом файл
(или создайте путь через редактор/`echo`, если родитель уже есть).

## `touch` — создать пустой файл

`touch practice/notes.txt` создаёт файл нулевой длины. Если файл уже есть, в настоящем
Unix обновилось бы время изменения; в песочнице достаточно знать: появляется пустой файл,
готовое «место» под текст через `echo` или `nano`.

## `echo` и перенаправление в файл

`echo текст` просто печатает строку в терминал.

`echo GitPlayground > practice/notes.txt` **записывает** строку в файл. Один `>` полностью
**перезаписывает** файл (старое содержимое пропадает).

`echo sandbox >> practice/notes.txt` **дописывает** строку в конец (`>>`).

Запомните пару:

| Операция | Символ | Эффект |
| --- | --- | --- |
| Перезапись | `>` | файл = только новая строка |
| Допись | `>>` | старое остаётся, новая строка в конце |

## `type nul >` — пустой файл

Форма `type nul > practice/blank.txt` знакома пользователям Windows: создаёт пустой файл.
В песочнице это разрешённый эквивалент «пустого» файла рядом с `touch`.

## `cat` — прочитать файл

`cat` печатает содержимое файла в терминал целиком — без открытия редактора.

```text
cat practice/notes.txt
```

Так проверяют результат `echo`, `>>` или сохранения из `nano`: на экране должно появиться
ровно то, что лежит на диске. В задачах уровня 0 файл `practice/notes.txt` уже может быть
в стартовом репозитории — достаточно прочитать его этой командой.

Если файла нет, песочница сообщит об ошибке. Сначала создайте файл (`touch` / `echo` /
`nano`), затем снова вызовите `cat`.

Отличие от `head` / `tail`: `cat` показывает **весь** файл; для длинных логов удобнее края.

## `head` и `tail` — края файла

`head -n 1 practice/notes.txt` — первые N строк (здесь одна).
`tail -n 1 practice/notes.txt` — последние N строк.

Удобно для длинных файлов и логов: не читать всё через `cat`, а глянуть начало или конец.
В задачах уровня часто достаточно одной строки (`-n 1`).

## `wc -l` — число строк

`wc -l practice/notes.txt` считает **строки** в файле. В полном Unix у `wc` есть режимы
слов и байт; в учебной песочнице для заданий достаточно строк.

## `cp` и `mv` — копия и переименование

`cp practice/notes.txt practice/copy.txt` — **копия**: исходник остаётся.
`mv practice/copy.txt practice/backup.txt` — **переименование или перенос**: старого имени
больше нет, содержимое на новом пути.

Сначала убедитесь через `ls`, что исходный файл существует.

## `find` — обход каталога

`find practice` рекурсивно перечисляет пути внутри каталога. Служебный `.git` песочница
пропускает. Полезно, когда файлов много и `ls` одной папки уже недостаточно.

## `rm` — удалить файл

`rm practice/blank.txt` удаляет файл с диска. В песочнице нельзя выйти за пределы `~/repo`
и нельзя снести `.git`. Перед удалением посмотрите `ls` — ошибочный `rm` не восстанавливается
из «Корзины».

## `nano` / `edit` — редактор

`nano practice/diary.txt` (или `edit …`) открывает **встроенный редактор** поверх терминала.
Это не системный nano, а учебный overlay:

- Ctrl+S / Ctrl+O — сохранить
- Ctrl+X — выйти в командную строку
- обычная вставка текста поддерживается в поле редактора

Подходит для многострочных файлов, когда `echo` неудобен.

## `clear` — очистить экран

`clear` или Ctrl+L очищает видимую область терминала. Файлы и история команд на сервере
не затрагиваются: меняется только то, что вы видите. Для зачётной задачи команда всё равно
фиксируется в журнале песочницы.

## Что такое Git (кратко)

**Git** — программа для **истории изменений** в проекте. Она запоминает снимки файлов
(коммиты), кто и когда их сделал, и позволяет работать в команде через ветки и слияния.

| Понятие | Простыми словами |
| --- | --- |
| **Репозиторий** | Папка проекта + скрытый каталог `.git` с историей |
| **Коммит** | Сохранённый снимок файлов с сообщением |
| **Рабочая папка** | Файлы, которые вы видите и редактируете прямо сейчас |

На **уровне 1** вы начнёте с `git init`, `git add`, `git commit`. Сейчас достаточно
понимать: Git **не заменяет** терминал — он **дополняет** его своими командами `git …`.

## Типичный путь новичка

1. Научиться смотреть, где вы находитесь (`pwd`, `ls`).
2. Создавать папки и файлы (`mkdir`, `touch`, `echo … >`).
3. Читать и копировать файлы (`cat`, `cp`, `mv`).
4. Перейти к Git: инициализировать репозиторий и сделать первый коммит.

Задачи этого уровня идут в том же порядке: сначала ориентируемся в папке, потом создаём
файлы, читаем, копируем и удаляем — всё теми же командами, что разрешены в песочнице.
"""

LEVEL_0_HINTS = (
    "Начни с pwd и ls — так проще ориентироваться в пустой папке.",
    "Для файла из нескольких строк удобен nano: откроется редактор прямо в браузере.",
)

LEVEL_0_DIAGRAM = """flowchart TB
  user[Вы в браузере] --> term[Терминал песочницы]
  term --> fs[Файлы и папки ~/repo]
  fs --> next[Уровень 1: git init и коммиты]
"""
